import { TitleFormatePipe } from './title-formate.pipe';

describe('TitleFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
